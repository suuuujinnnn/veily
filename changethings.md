# Change Things

이번 PR은 상담 카드 작성, 플래너-부부 일정 조율, 추천 업체 분류, 계약 관리, 고객 공유 페이지 흐름을 함께 정리한 변경입니다. 변경 범위가 넓어서 화면/흐름별로 어떤 기능이 어디에 들어갔는지 정리했습니다.

## 상담 카드

### 새 커플 등록 화면

- 위치: `src/features/couples/NewCouplePage.tsx`
- 새 커플 등록 폼 아래에 상담 카드 입력 영역을 추가했습니다.
- 플래너가 커플을 직접 등록할 때 상담 카드의 기본 내용을 함께 입력할 수 있도록 했습니다.
- 등록 완료 시 커플 데이터와 상담 카드 데이터가 함께 저장되도록 연결했습니다.
- `신랑신부가 직접 작성하기` 버튼을 추가해 공개 상담 카드 링크를 생성할 수 있도록 했습니다.
- 생성된 링크는 복사하거나 새 창으로 열 수 있도록 구성했습니다.

### 신랑신부 직접 작성 상담 카드

- 위치: `src/features/consultation/PublicConsultationCardPage.tsx`
- 공개 링크(`/consultation/new-*`)로 접근하는 상담 카드 작성 페이지를 추가했습니다.
- 신랑신부가 작성 완료하면 입력 내용을 기반으로 새 커플 데이터가 생성되도록 구성했습니다.
- 기존 커플 링크로 접근한 경우에는 해당 커플의 상담 카드로 저장되도록 했습니다.
- 마지막 제출 버튼 문구를 `작성 완료`로 변경했습니다.
- 작성 완료 후 해당 고객 공유 페이지(`/portal/:id`)로 이동하도록 연결했습니다.

### 상담 카드 입력 항목

- 위치: `src/features/consultation/PublicConsultationCardPage.tsx`, `src/types/index.ts`
- 첨부된 상담 카드 양식을 기준으로 다음 항목을 반영했습니다.
- 예식일정, 촬영 일정, 신랑신부 성함, 핸드폰번호, 미리 선별 업체를 입력할 수 있습니다.
- 스튜디오 방향, 스튜디오 무드, 드레스 무드, 메이크업 무드는 선택지와 직접 입력을 함께 지원합니다.
- 신랑신부 사이즈, 스드메 예산, 타 컨설팅 플래너 상담 여부, 추가 플래닝 희망 항목을 입력할 수 있습니다.
- 웨딩홀 관련 내용, 대면 상담 희망 일정, 연락 선호 방식, 기타 상담 메모를 입력할 수 있습니다.
- 상담 카드 타입을 확장해 위 항목들이 저장될 수 있도록 했습니다.

### 플래너 커플 상세 요약

- 위치: `src/features/couples/CoupleDetailPage.tsx`
- 상담 카드 내용은 `한눈에 보기` 탭에서만 보이도록 제한했습니다.
- 노출 순서를 `상담 카드 → 플래너 노트 → 커플 요약` 순으로 정리했습니다.
- 상담 카드 요약에는 희망 예식일, 촬영 일정, 스튜디오 취향, 드레스/메이크업 취향, 예산, 메모가 표시됩니다.

## 일정 조율

### 일정 데이터 흐름

- 위치: `src/app/store.tsx`, `src/types/index.ts`, `src/data/mockData.ts`
- 일정 승인 상태를 `planner-proposed`, `client-ok`, `confirmed`로 관리하도록 추가했습니다.
- 기존 mock 일정은 확정 일정으로 처리되도록 초기 데이터를 보정했습니다.
- 새로 등록되는 일정 조율 건은 기본적으로 `planner-proposed` 상태로 생성됩니다.

### 플래너 부부 상세 일정 조율

- 위치: `src/features/couples/CoupleDetailPage.tsx`
- 부부 상세의 `일정 & 할 일` 탭에 플래너 일정 제안 폼을 추가했습니다.
- 일정명, 유형, 날짜, 시작 시간, 종료 시간, 장소를 입력해 조율 일정을 만들 수 있습니다.
- 일정 유형은 상담, 스튜디오, 드레스, 메이크업, 기타를 선택할 수 있습니다.
- 고객이 오케이한 일정은 플래너 화면에서 `최종 확정` 버튼으로 확정할 수 있습니다.
- 확정된 일정은 조율 목록에서 빠지도록 정리했습니다.

### 플래너 일정 조율 캘린더

- 위치: `src/features/couples/CoupleDetailPage.tsx`, `src/styles/ux-polish.css`
- 플래너 부부 상세의 일정 조율 영역에 월간 캘린더 형태의 보기 화면을 추가했습니다.
- 제안 일정과 고객 오케이 상태의 미확정 일정은 주홍색으로 표시됩니다.
- 최종 확정된 일정은 조율 영역에서 제외되며, 전체 일정 흐름에서만 관리되도록 했습니다.
- 캘린더는 2026년 8월 월간 그리드 형태로 표시되며, 날짜별 일정명을 확인할 수 있습니다.

### 고객 공유 일정 조율

- 위치: `src/features/portal/PortalPage.tsx`
- 고객 공유 페이지의 일정 영역을 제휴 업체 일정이 아니라 플래너와 부부 사이의 일정 조율 중심으로 변경했습니다.
- 고객은 플래너가 제안한 일정을 확인하고 `오케이`할 수 있습니다.
- 주간 보기와 월간 보기 전환을 지원합니다.
- 월간 보기는 일반 캘린더 형태에 가깝게 일정이 날짜 칸에 표시됩니다.
- 일정 조율 탭과 확정 일정 탭을 분리했습니다.

### 전체 캘린더 반영 기준

- 위치: `src/features/calendar/CalendarPage.tsx`, `src/features/calendar/AddEventModal.tsx`
- 전체 캘린더에는 `confirmed` 상태의 일정만 보이도록 변경했습니다.
- 플래너 제안 또는 고객 오케이 상태의 미확정 일정은 전체 캘린더에서 제외됩니다.
- 일정 충돌 검사도 확정 일정 기준으로 동작하도록 변경했습니다.
- 일정 등록과 일정 조율 등록 시 겹치는 일정은 등록할 수 없도록 기존 방어 로직을 유지했습니다.

## 업체 추천

### 플래너 추천 업체 관리

- 위치: `src/features/couples/CoupleDetailPage.tsx`, `src/features/couples/CouplesPage.tsx`
- 플래너가 관리하는 부부 상세의 추천 업체 영역에 카테고리 탭을 추가했습니다.
- 추천 업체를 전체, 스튜디오, 드레스, 메이크업, 예물 기준으로 나누어 볼 수 있습니다.
- 추천 업체 카드에는 고객 응답 상태가 함께 표시됩니다.

### 고객 공유 추천 업체

- 위치: `src/features/portal/PortalPage.tsx`
- 고객 공유 페이지의 추천 업체 목록에도 카테고리 탭을 추가했습니다.
- 고객은 카테고리별로 추천 업체를 확인하고 `마음에 들어요` 또는 `조금 더 볼게요`로 응답할 수 있습니다.
- 응답 상태는 플래너 페이지의 추천 업체 카드에도 반영됩니다.

### 확정 업체 홈 노출

- 위치: `src/features/portal/PortalPage.tsx`
- 고객 공유 홈에 확정 업체 영역을 추가했습니다.
- 확정 업체는 스드메, 혼수, 예식장으로 나누어 표시됩니다.
- 스드메는 고객이 추천 업체에서 마음에 든 업체를 중심으로 보여주고, 혼수/예식장은 계약 데이터 기반으로 보여주도록 구성했습니다.

### 업체 상세 일정 제거

- 위치: `src/features/vendors/VendorDetailPage.tsx`
- 업체 상세 페이지의 주간 일정 영역을 제거했습니다.
- 업체별 일정 선택이 아니라 플래너와 부부 사이의 일정 조율 흐름으로 이동하도록 정리했습니다.

## 계약 관리

### 계약 탭 재구성

- 위치: `src/features/contracts/ContractsSection.tsx`, `src/features/couples/CoupleDetailPage.tsx`
- 부부 상세의 `계약` 탭을 전용 컴포넌트로 분리했습니다.
- 계약 관리는 스드메, 혼수, 예식장, 입금내역 중심으로 재구성했습니다.
- 계약 탭에서 카테고리별 계약 목록을 볼 수 있습니다.

### 계약 목록 테이블

- 위치: `src/features/contracts/ContractsSection.tsx`
- 계약 목록을 표 형태로 구성했습니다.
- 표 컬럼은 계약일, 품목, 업체명, 계약서, 상품명, 세부 구성, 결제, VAT, 계약총액, 수수료, 입금일, 상태, 관리입니다.
- 각 계약 행에서 수정과 삭제가 가능합니다.
- 스드메, 혼수, 예식장 그룹별로 등록 건수와 요약 금액을 표시합니다.

### 스드메 등록 팝업

- 위치: `src/features/contracts/ContractsSection.tsx`, `src/styles/ux-polish.css`
- 계약 등록 버튼을 누르면 상세 등록 팝업이 열리도록 구현했습니다.
- 팝업은 스크린샷을 참고해 넓은 2열 업무 폼 형태로 디자인을 조정했습니다.
- 팝업 헤더와 푸터는 스크롤 중에도 안정적으로 보이도록 고정형 스타일을 적용했습니다.
- 입력 필드 간격, 높이, 포커스 색상, 파일 입력, textarea 스타일을 정리했습니다.
- 입력 가능한 항목은 구분, 품목, 계약일, 업체명, 계약서 사본, 계약서 파일명, 상품명, 결제수단, VAT, 계약총액, 수수료, 계약금, 입금일, 상태, 세부적인 구성, 수수료정보, 비고, 입금 계정, 입금 상태, 입금 비고입니다.

### 입금내역

- 위치: `src/features/contracts/ContractsSection.tsx`
- 계약 관리 안에 `입금내역` 카테고리를 별도로 추가했습니다.
- 입금내역 표에는 입금일, 타입, 계정, 금액, 비고, 상태, 관리 항목을 표시합니다.
- 입금내역에서도 수정과 삭제가 가능합니다.
- 하단에 총계약금, 입금, 잔금 요약을 추가했습니다.

### 계약 데이터 타입

- 위치: `src/types/index.ts`
- 계약 상세 화면에서 필요한 필드를 저장할 수 있도록 계약 타입을 확장했습니다.
- 품목, 계약일, 계약서 파일명, 상품명, 계약금, 수수료, 수수료정보, 비고, 입금일, 입금 타입, 입금 계정, 입금 비고, 입금 상태를 선택 필드로 추가했습니다.
- 스드메, 혼수 카테고리와 `미입력` 결제수단도 처리할 수 있도록 타입을 보강했습니다.

## 고객 공유 페이지

### 공유 페이지 진입 흐름

- 위치: `src/features/portal/PortalEntryPage.tsx`, `src/app/App.tsx`
- 고객 공유 페이지 진입 전의 `준비 현황 전체 보기 / 캘린더로 보기` 중간 화면을 제거했습니다.
- 기존 `/client/:id` 진입은 바로 `/portal/:id`로 이동하도록 정리했습니다.
- 고객 공유 페이지 라우팅과 공개 상담 카드 라우팅을 추가했습니다.

### 고객 링크 복사

- 위치: `src/features/couples/CoupleDetailPage.tsx`
- 플래너의 고객 화면 미리보기 버튼 아래에 고객 페이지 URL을 복사할 수 있는 입력 영역을 추가했습니다.
- 링크 복사 후 버튼 문구가 `복사됨`으로 잠시 바뀌도록 했습니다.

### 고객 공유 홈

- 위치: `src/features/portal/PortalPage.tsx`
- 부부 이름에 `님` 호칭이 붙도록 표시를 조정했습니다.
- 고객 공유 홈에서 다가오는 일정, 준비율, 추천 업체 안내, 확정 업체 그룹을 볼 수 있도록 정리했습니다.
- 확정 업체 그룹은 스드메, 혼수, 예식장으로 나누어 표시됩니다.

### 고객 공유 할 일

- 위치: `src/features/portal/PortalPage.tsx`
- 부부 공유 탭의 할 일은 기본적으로 비어 있는 상태로 시작하도록 변경했습니다.
- 첫 진입 시 `기본 템플릿을 사용해 볼까요?` 안내를 띄우도록 했습니다.
- 기본 템플릿을 선택하면 기존 월별 로드맵과 분야별 체크리스트가 표시됩니다.
- 빈 화면에서 시작을 선택하면 기본 템플릿 없이 직접 항목을 추가할 수 있습니다.

## 플래너 할 일 관리

- 위치: `src/features/couples/CoupleDetailPage.tsx`
- 플래너의 부부 상세 `일정 & 할 일` 탭에도 템플릿 사용 또는 직접 작성 선택지를 추가했습니다.
- 템플릿 사용을 선택하면 기존 월별 로드맵과 분야별 체크리스트가 표시됩니다.
- 직접 작성을 선택하면 템플릿 항목은 숨기고 직접 추가한 할 일 중심으로 관리합니다.
- 분야별 추가/수정/삭제는 기존 체크리스트 편집 모달과 연결했습니다.

## 상태 저장과 mock 데이터

- 위치: `src/app/store.tsx`, `src/data/mockData.ts`
- 커플, 일정, 체크리스트, 추천 업체, 상담 카드, 계약 데이터를 store에서 관리하도록 확장했습니다.
- 새 상담 카드 작성 후 생성된 고객 데이터가 이어지도록 localStorage 저장을 적용했습니다.
- 초기 상담 카드와 계약 데이터 샘플을 추가했습니다.
- 추천 업체, 계약, 일정 승인 상태가 여러 화면에서 같은 데이터를 바라보도록 연결했습니다.

## UI 스타일

- 위치: `src/styles/ux-polish.css`, `src/main.tsx`
- 상담 카드, 고객 링크 복사, 추천 업체 카테고리 탭, 일정 상태, 계약 테이블, 계약 상세 팝업, 입금내역, 고객/플래너 일정 조율 캘린더 스타일을 추가했습니다.
- 새 스타일 파일을 앱 엔트리에서 import하도록 연결했습니다.
- 모바일 화면에서 계약 팝업, 캘린더, 카테고리 탭이 무너지지 않도록 반응형 스타일을 추가했습니다.

## 검증

- `npm run build`로 TypeScript 빌드와 Vite 프로덕션 빌드를 확인했습니다.
- 로컬 dev 서버 `http://127.0.0.1:5173/` 응답을 확인했습니다.
