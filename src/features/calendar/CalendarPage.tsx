import { Fragment, useMemo, useState } from 'react'
import { Building2, CalendarPlus, Car, ChevronLeft, ChevronRight, Home, MapPin, Plus, Settings2 } from 'lucide-react'
import { useDemoStore } from '../../app/store'
import { Badge, Button, Card } from '../../components/ui'
import type { WeddingEvent } from '../../types'
import { AddEventModal } from './AddEventModal'
import { buildTravelPlans, findCalendarConflicts, type TransitPreference } from './calendarUtils'
import { TravelLegBlock } from './TravelLegBlock'

const eventTypes = ['미팅', '스튜디오', '드레스', '메이크업', '계약', '본식']
const eventClass: Record<string, string> = { 미팅: 'meeting', 스튜디오: 'studio', 드레스: 'dress', 메이크업: 'makeup', 계약: 'contract', 본식: 'ceremony' }
const minutes = (time: string) => Number(time.slice(0, 2)) * 60 + Number(time.slice(3, 5))

export function CalendarPage() {
  const { events, couples } = useDemoStore()
  const [view, setView] = useState<'month' | 'week'>('month')
  const [filter, setFilter] = useState('전체')
  const [selectedDate, setSelectedDate] = useState('2026-08-05')
  const [transitPreference, setTransitPreference] = useState<TransitPreference>('bus')
  const [baseLocation, setBaseLocation] = useState('VEILY 스튜디오 · 서울')
  const [useCar, setUseCar] = useState(true)
  const [modalOpen, setModalOpen] = useState(false)
  const [toast, setToast] = useState(false)
  const conflicts = useMemo(() => findCalendarConflicts(events), [events])
  const filteredEvents = useMemo(() => events.filter((event) => filter === '전체' || event.type === filter), [events, filter])
  const selectedEvents = useMemo(() => filteredEvents.filter((event) => event.date === selectedDate).sort((a, b) => a.time.localeCompare(b.time)), [filteredEvents, selectedDate])
  const travelPlans = useMemo(() => buildTravelPlans(selectedEvents, baseLocation, transitPreference, useCar), [selectedEvents, baseLocation, transitPreference, useCar])
  const travelPlanByEvent = new Map(travelPlans.map((plan) => [plan.eventId, plan]))
  const selectedDateObject = new Date(`${selectedDate}T00:00:00`)
  const monthDays = Array.from({ length: 42 }, (_, index) => {
    const day = index - 5
    if (day < 1) return { day: 31 + day, current: false, date: `2026-07-${String(31 + day).padStart(2, '0')}` }
    if (day > 31) return { day: day - 31, current: false, date: `2026-09-${String(day - 31).padStart(2, '0')}` }
    return { day, current: true, date: `2026-08-${String(day).padStart(2, '0')}` }
  })
  const added = () => { setToast(true); window.setTimeout(() => setToast(false), 2200) }
  return <div className="page-stack calendar-page">
    <section className="page-intro"><div><p className="eyebrow">공유 캘린더</p><h1>일정</h1><p>확정된 일정과 이동수단을 한눈에 관리하세요.</p></div><Button icon={<CalendarPlus size={16} />} onClick={() => setModalOpen(true)}>새 일정 등록</Button></section>
    <section className="calendar-route-settings"><div className="calendar-route-settings__intro"><span><Settings2 size={18} /></span><div><strong>이동 기준 설정</strong><p>일정 사이 이동시간 계산에 사용할 기준을 설정합니다.</p></div></div><label><span>자주 이용하는 교통</span><select value={transitPreference} onChange={(event) => setTransitPreference(event.target.value as TransitPreference)}><option value="bus">버스 중심</option><option value="subway">지하철 중심</option><option value="car">자동차 중심</option></select></label><label><span>기준 위치</span><select value={baseLocation} onChange={(event) => setBaseLocation(event.target.value)}><option>VEILY 스튜디오 · 서울</option><option>지점 · 잠실</option><option>지점 · 성수</option></select></label><label className="car-toggle"><input type="checkbox" checked={useCar} disabled={transitPreference === 'car'} onChange={(event) => setUseCar(event.target.checked)} /><span><Car size={14} /></span><div><strong>자동차 보정</strong><small>일부 구간 자동차 이동</small></div></label></section>
    <div className="calendar-toolbar"><div className="month-controller"><button aria-label="이전 달"><ChevronLeft size={18} /></button><h2>2026년 8월</h2><button aria-label="다음 달"><ChevronRight size={18} /></button><button className="today-button" onClick={() => setSelectedDate('2026-08-05')}>오늘</button></div><div className="calendar-view-toggle"><button onClick={() => setView('month')} className={view === 'month' ? 'active' : ''}>월</button><button onClick={() => setView('week')} className={view === 'week' ? 'active' : ''}>주</button></div></div>
    <div className="calendar-filter-row"><span>일정 유형</span><button onClick={() => setFilter('전체')} className={filter === '전체' ? 'active' : ''}>전체</button>{eventTypes.map((item) => <button key={item} onClick={() => setFilter(item)} className={filter === item ? 'active' : ''}><i className={`filter-dot filter-dot--${eventClass[item]}`} />{item}</button>)}</div>
    <div className="calendar-layout calendar-layout--routes"><Card padding="none" className="month-calendar"><div className="week-header">{['일', '월', '화', '수', '목', '금', '토'].map((name) => <span key={name}>{name}</span>)}</div><div className={`month-grid ${view === 'week' ? 'month-grid--week' : ''}`}>{monthDays.map((item, index) => { if (view === 'week' && (index < 7 || index > 13)) return null; const dayEvents = filteredEvents.filter((event) => event.date === item.date).sort((a, b) => a.time.localeCompare(b.time)); const dayPlans = buildTravelPlans(dayEvents, baseLocation, transitPreference, useCar); const planMap = new Map(dayPlans.map((plan) => [plan.eventId, plan])); const visibleEvents = dayEvents.slice(0, view === 'week' ? 5 : 3); const finalPlan = visibleEvents.length === dayEvents.length ? planMap.get(visibleEvents.at(-1)?.id ?? '') : undefined; return <div key={item.date} role="button" tabIndex={0} onClick={() => setSelectedDate(item.date)} onKeyDown={(event) => { if (event.key === 'Enter') setSelectedDate(item.date) }} className={`calendar-cell ${!item.current ? 'calendar-cell--muted' : ''} ${item.date === '2026-08-05' ? 'calendar-cell--today' : ''} ${selectedDate === item.date ? 'calendar-cell--selected' : ''}`}><div className="calendar-cell__top"><span>{item.day}</span>{item.date === '2026-08-05' && <small>오늘</small>}<button onClick={(event) => { event.stopPropagation(); setSelectedDate(item.date); setModalOpen(true) }} aria-label={`${item.day}일 일정 추가`}><Plus size={13} /></button></div><div className="calendar-cell__events calendar-cell__events--timeline">{visibleEvents.map((event) => { const plan = planMap.get(event.id); return <Fragment key={event.id}>{plan && <TravelLegBlock leg={plan.before} position="before" />}<div className={`calendar-event calendar-event--${eventClass[event.type] ?? 'meeting'}`}><strong>{event.time}</strong><span>{event.title}</span></div></Fragment> })}{finalPlan && <TravelLegBlock leg={finalPlan.after} position="after" />}{dayEvents.length > visibleEvents.length && <small>+{dayEvents.length - visibleEvents.length}건 더 보기</small>}</div></div> })}</div></Card><aside className="day-panel day-panel--routes"><div className="day-panel__heading"><div><span>{String(selectedDateObject.getDate()).padStart(2, '0')}</span><p><strong>{selectedDateObject.toLocaleDateString('ko-KR', { weekday: 'long' })}</strong><small>{selectedDateObject.getFullYear()}년 {selectedDateObject.getMonth() + 1}월</small></p></div><Badge tone="rose">{selectedEvents.length} 일정</Badge></div><div className="day-schedule">{selectedEvents.length ? selectedEvents.map((event, index) => { const couple = couples.find((item) => item.id === event.coupleId); const plan = travelPlanByEvent.get(event.id); return <div className="day-route-group" key={event.id}>{plan && <TravelLegBlock leg={plan.before} position="before" variant="panel" />}<div className={`day-event day-event--${eventClass[event.type] ?? 'meeting'}`}><span className={`day-event__line day-event__line--${eventClass[event.type] ?? 'meeting'}`} /><div className="day-event__time"><strong>{event.time}</strong><span>{event.endTime}</span></div><div className="day-event__body"><span className={`event-type-pill event-type-pill--${eventClass[event.type] ?? 'meeting'}`}>{event.type}</span><h3>{event.title}</h3><p>{couple?.partners}님</p><small><MapPin size={12} /> {event.location}</small></div></div>{plan && index === selectedEvents.length - 1 && <TravelLegBlock leg={plan.after} position="after" variant="panel" />}</div> }) : <div className="day-empty"><Building2 size={20} /><strong>등록된 일정이 없습니다.</strong><p>오른쪽 위 버튼으로 새 일정을 추가해 보세요.</p></div>}</div><div className="day-base-location"><Home size={13} /><span>기준 위치</span><strong>{baseLocation}</strong></div><button className="day-panel__add" onClick={() => setModalOpen(true)}><Plus size={15} /> 새 일정 추가</button></aside></div>
    <AddEventModal open={modalOpen} onClose={() => setModalOpen(false)} onAdded={added} />{toast && <div className="toast"><span>✓</span><div><strong>일정이 등록되었습니다.</strong><p>캘린더에 새 일정이 반영되었습니다.</p></div></div>}
  </div>
}